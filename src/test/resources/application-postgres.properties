spring.sql.init.platform=postgres
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost/petmanagement-test}
spring.datasource.username=${POSTGRES_USER:petmanagement-test}
spring.datasource.password=${POSTGRES_PASS:petmanagement-test}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always